name: 'dbt_something'
version: '0.1'
profile: 'default'
config-version: 2

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  dbt_something:
      materialized: table

sources:
  - name: jaffle_shop
    tables:
      - name: customers
